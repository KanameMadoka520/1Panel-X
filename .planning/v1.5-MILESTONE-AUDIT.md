---
milestone: v1.5
audited: 2026-07-13T18:45:00-04:00
status: gaps_found
scores:
  requirements: 0/3
  phases: 3/3
  integration: 1/1
  flows: 0/1
security_review: passed (0 confirmed defects; workflow wf_349d7556-0ef, 10 agents; 4 raw findings all refuted, 3 adopted as hardening N7/N10/N13)
gaps:
  requirements:
    - id: NODE-ENROLL-01
      status: partial
      phase: 13-node-registry-pki-enrollment
      verification_status: human_needed
      evidence: nodepki 10 tests (CA chain, CN imposition N13, token replay/forgery/expiry N1/N2/N3, single-box loopback mTLS pin N5/N6/N8), node service 4 tests (N14 SSRF addr, port, N1 replay-reject, N2 forged, N10 revoke), gofmt/vet/build clean, full core go test exit 0. Cross-network live enrollment is Slice C (needs 2nd VPS).
    - id: NODE-PROXY-01
      status: partial
      phase: 14-mtls-proxy-agent-validate-bootstrap
      verification_status: human_needed
      evidence: core helper 3 tests (loopback mTLS pin N5, unenrolled/revoked reject N10, resolve), agent nodepki 4 tests, agent helper 3 tests (IsProvisionedNode default-master exhaustive, ApplyEnrollment, ValidateCertificate pin N6); both modules build/vet/test clean. Live cross-network proxy + node-mode boot + single-host non-regression on a real box are Slice C.
    - id: NODE-UI-01
      status: partial
      phase: 15-community-node-ui
      verification_status: human_needed
      evidence: ESLint 0, build:pro OK, honest admin-gate (no license forged), build did not touch embedded web assets. Live browser add->token/list/revoke/delete is human UAT.
  integration: []
  flows:
    - name: End-to-end secure node federation (enroll -> mTLS proxy -> operate)
      reason: No live two-box run — a node joining over the public internet, booting into node mode, and core proxying a real operation to it. Requires a second VPS (Slice C) + the node-side join trigger (CLI/UI).
tech_debt:
  - phase: 14-mtls-proxy-agent-validate-bootstrap
    items:
      - Live enroll HTTP call + node-mode boot are not wired to an operator trigger (Slice C).
      - LoadNodeInfo SSH-based remote access left as a no-op (token+mTLS model collects no SSH creds).
  - phase: 15-community-node-ui
    items:
      - Top-bar node switcher (operate flow) not re-gated — it is pro-coupled (status vocab "Healthy", node roles, absent NodeDashboard page); v1.5 ships a dedicated community management page instead. Operate flow is Slice C.
      - UI i18n zh + en only.
  - milestone: v1.5
    items:
      - Running full core go test ./... regenerates both x-log.json from the community build (strips enterprise/xpack entries); reverted. Release uses focused tests.
carried_debt:
  - milestone: v1.0
    note: 20 human UAT items still pending; not archived.
  - milestone: v1.1
    note: 6 human UAT items still pending; not archived.
  - milestone: v1.2
    note: 4 human UAT items still pending; not archived.
  - milestone: v1.3
    note: BRAND-IMG-01 verified live 2026-07-11; image-form visual pending. Not archived.
  - milestone: v1.4
    note: 2 human UAT items (login-text render, hero-fix visual) pending. Not archived.
deferred:
  - Slice C — cross-network live acceptance of secure multi-node (needs a second VPS + a node-side join trigger).
---

# Milestone v1.5 Audit: Secure Multi-Node (keystone)

## Result

**Status:** `gaps_found`

All three phases are implemented, automatically verified across both Go modules and the frontend, and cleared a 0-confirmed-defect adversarial security review (10 agents; the 4 refuted findings were adopted as hardenings N7/N10/N13). The source-to-release chain is reproducible (`v1.5.0-open.1`, `dirty=false`). Not "done" because each requirement needs live evidence that only a second host provides — a node actually joining over the network, booting into node mode, and core proxying a real operation to it — plus browser UAT. Same honest posture as every prior milestone.

## Requirement Cross-Reference

| Requirement | REQUIREMENTS.md | Verification | Final |
|-------------|-----------------|--------------|-------|
| NODE-ENROLL-01 | Human UAT Pending | automated PASS | Partial |
| NODE-PROXY-01 | Human UAT Pending | automated PASS | Partial |
| NODE-UI-01 | Human UAT Pending | automated PASS | Partial |

## Automated Evidence (passed)
- Core: nodepki 10, node service 4 (incl. N10 revoke), helper 3 tests; full core `go test ./...` exit 0; gofmt/vet/build clean.
- Agent: nodepki 4, helper 3 tests (incl. N6 ValidateCertificate, IsProvisionedNode default-master); build/vet clean.
- Frontend: changed-file ESLint 0; `npm run build:pro` OK; embedded web assets untouched.

## Security posture (adversarial review — passed)
Workflow `wf_349d7556-0ef` (10 agents: 6 lenses — enroll-token, core-PKI-proxy, agent-side, single-host-safety regression, auth-gating, completeness-critic — → per-finding adversarial verify). **0 confirmed exploitable defects**; 4 raw findings all high-confidence refuted. Adopted as hardening:
- **N7:** core now unconditionally strips inbound `X-Panel-User` before the conditional set.
- **N10:** real `Revoke` (row preserved for audit) + dial path refuses revoked/offline (was doc-overclaimed dead code).
- **N13:** enroll endpoint rate-limited (per-IP fixed window, dependency-free) + success audit log.
- **N4:** confirmed honest — agent `EnrollTLSConfig("")` is fail-closed; documented best-effort. No change.

## Release Evidence
- Release: `image/releases/v1.5.0-open.1` (does not overwrite v1.0–v1.4, all re-verified OK).
- Source revision: `6d363f6efff5ff9fc7a2718c9095b702b7898f5d`, `dirty=false`. Upstream base `8be2a9ab0`; officialBaseTag `v2.2.3`.
- Checksums: top-level 8/8, in-archive 7/7. Static stripped linux/amd64 ELF. VPS `not_run_by_build_script`. No `.build-*` residue; no proprietary overlay; no license bypass.
- Core `b39299ff5b44b4e065be2e689fd888e143f0f5dbd70bf515e8064f9301eb8669`
- Agent `b0fbe93f876778d5b400530b4c8ce352c6c1c4d9c66b5076e322ecc2ff00b15f` (**new** — v1.5 changed agent source: nodepki + ValidateCertificate/LoadNodeInfo + enroll; the byte-identity with v1.1–v1.4 no longer holds, as expected)
- Archive `bcd06a7eafd5e65ab49ec4d1b6e1c4cef8373702b9adcdedf9b6816eb6546e35`

## Decision

Do not archive v1.5. Transfer `v1.5.0-open.1` to two VPSs, execute the Slice C live acceptance (enroll a node over the network → it boots into node mode → core proxies a real operation) + the browser UAT, record results, and re-run the audit. The branding cluster (v1.2–v1.4) plus the multi-node backend + community UI are now in place; the remaining work is genuinely two-host.
