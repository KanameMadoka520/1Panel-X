---
phase: 05-reproducible-release-and-vps-handoff
plan: 01
type: execute
wave: 1
depends_on:
  - 01-01
  - 02-01
  - 03-01
  - 04-01
files_modified:
  - .planning/phases/05-reproducible-release-and-vps-handoff/05-01-SUMMARY.md
  - .planning/phases/05-reproducible-release-and-vps-handoff/05-VERIFICATION.md
  - .planning/phases/05-reproducible-release-and-vps-handoff/05-HUMAN-UAT.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - RELEASE-01
user_setup:
  - A disposable or fully snapshotted Linux VPS is required for deferred installation, browser, provider, EICAR, capacity, and rollback acceptance.
must_haves:
  truths:
    - Exact documented Linux toolchains can build the committed source revision from a clean worktree.
    - The external release directory contains native binaries, an archive, two checksum layers, logs, metadata, and VPS instructions.
    - Metadata and checksums identify the source revision, upstream base, dependency locks, target architecture, and automated verification results.
    - VPS instructions cover prerequisites, consistent backup, atomic replacement, startup, smoke tests, feature acceptance, and full rollback risk.
    - Deferred live acceptance remains visible as human UAT, and no unperformed check is marked passed.
  artifacts:
    - path: D:/_CodeNotSync/_1Panel-X/image/releases/v1.0.0-open.1
      provides: Versioned Linux AMD64 release candidate and evidence.
    - path: D:/_CodeNotSync/_1Panel-X/image/README-VPS.md
      provides: VPS deployment, acceptance, and rollback instructions.
    - path: .planning/phases/05-reproducible-release-and-vps-handoff/05-VERIFICATION.md
      provides: Automated release gate and honest human-verification status.
    - path: D:/_CodeNotSync/_1Panel-X/roadmap
      provides: Timestamped Chinese release record outside the source tree.
  key_links:
    - from: D:/_CodeNotSync/_1Panel-X/image/build-release.sh
      to: D:/_CodeNotSync/_1Panel-X/source
      via: clean source revision, exact toolchains, tests, and native builds
    - from: D:/_CodeNotSync/_1Panel-X/source
      to: RELEASE-METADATA.json
      via: Git revision, merge base, dependency-lock hashes, and build environment
    - from: README-VPS.md
      to: 1panel-core and 1panel-agent
      via: checksum verification, backup, atomic replacement, startup, and rollback
    - from: 05-VERIFICATION.md
      to: 05-HUMAN-UAT.md
      via: persisted live acceptance items
---

<objective>
Build, inspect, package, and document the v1.0.0-open.1 Linux AMD64 release candidate from an exact committed source revision.

Purpose: Give an operator a reproducible and auditable VPS handoff without proprietary overlays or false claims of live acceptance.
Output: External release artifacts and checksums, source-side release evidence, a timestamped Chinese roadmap note, and persistent human UAT.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-reproducible-release-and-vps-handoff/05-CONTEXT.md
@D:/_CodeNotSync/_1Panel-X/image/build-release.sh
@D:/_CodeNotSync/_1Panel-X/image/README-VPS.md
@D:/_CodeNotSync/_1Panel-X/image/RELEASE-METADATA.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the locked Linux release pipeline</name>
  <files>D:/_CodeNotSync/_1Panel-X/image/releases/v1.0.0-open.1</files>
  <action>From a clean source revision, put the verified Linux Node 24.14.0 and npm 11.14.1 toolchain first on PATH, select Go 1.26.1, then run `build-release.sh` with the documented Go proxy and release version. Do not disable focused tests or package compile checks.</action>
  <verify>
    <automated>WSL release script exits zero and creates `image/releases/v1.0.0-open.1`.</automated>
  </verify>
  <done>The frontend build, focused Go tests, package compile checks, and native binary builds all pass in the release pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: Inspect provenance, archive, binaries, and checksums</name>
  <files>D:/_CodeNotSync/_1Panel-X/image/releases/v1.0.0-open.1</files>
  <action>Run top-level `sha256sum -c`, extract or stream-list the archive, run the in-archive checksum verification, confirm both binaries are nonempty Linux x86-64 ELF executables, inspect JSON metadata and logs, and confirm no `.build-*` directory remains. Compare metadata revision and identity to the current Git commit.</action>
  <verify>
    <automated>All top-level and in-archive checksum entries report OK; `file` reports Linux x86-64 ELF binaries; metadata reports clean source and passed automated gates.</automated>
  </verify>
  <done>The release candidate is internally consistent, attributable to one source revision, and ready for VPS transfer.</done>
</task>

<task type="auto">
  <name>Task 3: Record release evidence and deferred acceptance</name>
  <files>.planning/phases/05-reproducible-release-and-vps-handoff/05-01-SUMMARY.md, .planning/phases/05-reproducible-release-and-vps-handoff/05-VERIFICATION.md, .planning/phases/05-reproducible-release-and-vps-handoff/05-HUMAN-UAT.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, D:/_CodeNotSync/_1Panel-X/roadmap</files>
  <action>Record the exact release path, source revision, checksums, toolchains, automated results, and known limitations. Persist VPS, browser, provider, EICAR, capacity, and rollback checks as pending UAT. Update milestone documents to reflect a release candidate with automated gates passed but no full requirement closure. Create the external roadmap note using a local timestamp and Chinese summary filename.</action>
  <verify>
    <automated>`git diff --check`, GSD summary verification, `audit-uat --raw`, Git identity checks, and release-path inspection all pass.</automated>
  </verify>
  <done>The source history, external release artifacts, and roadmap record form one traceable handoff without claiming complete Pro parity or unperformed acceptance.</done>
</task>

</tasks>

<verification>
- The release script runs without skipped automated gates.
- Top-level and in-archive checksums pass.
- Metadata revision equals the source revision used for the build.
- Core and agent binaries are Linux AMD64 ELF files.
- Phase 5 verification uses `human_needed` until real VPS acceptance is performed.
- The external roadmap note follows the timestamp plus Chinese summary naming rule.
</verification>

<success_criteria>
- A reproducible `v1.0.0-open.1` release candidate exists under the external `image` directory.
- Automated build and package evidence is inspectable without relying on conversation history.
- VPS deployment and rollback instructions are complete enough for a disposable or snapshotted host.
- Pending human acceptance is machine-discoverable and blocks full requirement completion.
- All source commits retain the requested author and committer identity.
</success_criteria>

<output>
After completion, create `.planning/phases/05-reproducible-release-and-vps-handoff/05-01-SUMMARY.md`.
</output>
