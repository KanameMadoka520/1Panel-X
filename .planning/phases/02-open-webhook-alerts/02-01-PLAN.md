---
phase: 02-open-webhook-alerts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/app/service/alert.go
  - agent/app/service/alert_webhook_config_test.go
  - agent/utils/webhook_alert/sender.go
  - agent/utils/webhook_alert/sender_test.go
  - agent/utils/xpack/helper/alert.go
  - frontend/src/views/setting/alert/dash/task/index.vue
  - frontend/src/views/setting/alert/setting/drawer/index.vue
  - frontend/src/views/setting/alert/setting/index.vue
autonomous: true
requirements:
  - ALERT-01
user_setup:
  - Disposable WeCom, DingTalk, and Feishu or Lark robot URLs are required for deferred VPS acceptance.
must_haves:
  truths:
    - Community and international builds can configure and select the three webhook providers while SMS remains restricted.
    - Requests use provider-specific payloads and validate HTTP and business response codes.
    - Delivery uses hardened HTTPS transports with official-host validation, TLS verification, bounded responses, and no redirects.
    - HTTP and business response failures create error logs without exposing complete webhook URLs.
    - Settings responses mask stored webhook URLs.
    - Real provider delivery is manually accepted before ALERT-01 is closed.
  artifacts:
    - path: agent/utils/webhook_alert/sender.go
      provides: Provider payloads and hardened delivery.
    - path: agent/app/service/alert.go
      provides: Config availability, validation, masking, and masked-edit restoration.
    - path: agent/utils/xpack/helper/alert.go
      provides: Delivery integration and attempt logs.
    - path: frontend/src/views/setting/alert/setting/index.vue
      provides: Community provider availability in settings UI.
  key_links:
    - from: agent/app/service/alert.go
      to: agent/utils/webhook_alert/sender.go
      via: provider URL validation
    - from: agent/utils/xpack/helper/alert.go
      to: agent/utils/webhook_alert/sender.go
      via: webhook delivery call
    - from: agent/utils/xpack/helper/alert.go
      to: agent/utils/alert/alert.go
      via: success or error AlertLog persistence
---

<objective>
Deliver secure, license-independent WeCom, DingTalk, and Feishu robot webhook alerts.

Purpose: Satisfy the implementable portion of ALERT-01 with open protocols and auditable attempt results.
Output: Hardened sender, alert-service integration, unblocked UI, automated tests, and an explicit real-provider UAT handoff.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-open-webhook-alerts/02-CONTEXT.md
@agent/app/service/alert.go
@agent/utils/xpack/providers/alert.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the hardened provider sender</name>
  <files>agent/utils/webhook_alert/sender.go, agent/utils/webhook_alert/sender_test.go</files>
  <read_first>agent/utils/xpack/providers/alert.go, agent/app/dto/alert.go</read_first>
  <action>Implement WeCom and DingTalk `msgtype: text` payloads and Feishu `msg_type: text` payloads. Validate HTTPS official hosts and port 443, reject IPs and userinfo, clone transports with TLS verification and TLS 1.2 minimum, clear SNI overrides, reject redirects, enforce a 10-second timeout and 64 KiB response cap, and validate platform business response codes without returning the webhook URL.</action>
  <verify>Run `go test ./utils/webhook_alert -count=1` and the complete `go test ./...` gate from `agent` in Linux/WSL.</verify>
  <acceptance_criteria>
    - Sender tests cover payloads, allowed hosts, unsafe targets, TLS verification, cloned transport behavior, redirects, HTTP errors, timeouts, oversized/invalid responses, and Feishu legacy responses.
    - No sender error formats the webhook URL.
    - Agent tests exit 0.
  </acceptance_criteria>
  <done>The sender accepts only intended provider destinations and reports deterministic delivery results.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate configuration, masking, delivery, and attempt logs</name>
  <files>agent/app/service/alert.go, agent/app/service/alert_webhook_config_test.go, agent/utils/xpack/helper/alert.go</files>
  <read_first>agent/app/service/alert.go, agent/app/service/alert_helper.go, agent/utils/alert/alert.go, agent/utils/xpack/helper/alert.go</read_first>
  <action>Remove WeCom, DingTalk, and Feishu from the community-restricted method map while retaining SMS. Validate provider configs before persistence. Mask URLs in list/page responses and restore the original URL for masked edits. Send through `webhook_alert.Send`, persist `AlertSuccess` or `AlertError` for every attempt, truncate error messages, and rely on the persisted attempt log for quota accounting.</action>
  <verify>Run config masking tests and full `agent` package tests.</verify>
  <acceptance_criteria>
    - Community method restriction map contains SMS only.
    - Config list/page responses use `********` for non-empty webhook URLs.
    - Masked edits retain the existing stored URL.
    - Delivery failures persist an error AlertLog and do not include the secret URL.
  </acceptance_criteria>
  <done>Webhook providers are usable by community alerts and each attempted delivery is auditable.</done>
</task>

<task type="auto">
  <name>Task 3: Expose the three providers in the existing UI</name>
  <files>frontend/src/views/setting/alert/dash/task/index.vue, frontend/src/views/setting/alert/setting/drawer/index.vue, frontend/src/views/setting/alert/setting/index.vue</files>
  <read_first>frontend/src/views/setting/alert/setting/index.vue, frontend/src/views/setting/alert/setting/drawer/index.vue, frontend/src/views/setting/alert/dash/task/index.vue</read_first>
  <action>Change provider availability conditions so community and international builds can configure/select WeCom, DingTalk, and Feishu, while SMS remains disabled where licensing or region rules require it. Preserve masked URL editing behavior.</action>
  <verify>Run targeted ESLint for the three Vue files and run `npm run build:pro`.</verify>
  <acceptance_criteria>
    - License/region disable expressions apply to SMS only.
    - Targeted ESLint and production build exit 0.
  </acceptance_criteria>
  <done>The existing alert UI exposes the open webhook providers and compiles.</done>
</task>

</tasks>

<verification>
- [x] Linux/WSL `agent`: `go test ./...`
- [x] Targeted ESLint for Phase 2 frontend files
- [x] `frontend`: `npm run build:pro`
- [ ] Disposable real-robot sends from the VPS environment (deferred)
- [ ] Database at-rest encryption for webhook URLs (deferred technical debt, not claimed)
</verification>

<success_criteria>

- Automated sender, service, and frontend gates pass.
- Secrets are redacted from responses and delivery errors.
- Plaintext at-rest storage is disclosed.
- ALERT-01 remains pending until real-provider VPS acceptance passes.

</success_criteria>

<output>
Execution was committed in `47e0887ee61d75f8f6b17c12b9a0bec90a6e37e8`; the retrospective result is recorded in `02-01-SUMMARY.md`.
</output>
