---
phase: 03-scheduled-clamav
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/app/service/clam.go
  - agent/app/service/clam_schedule_test.go
  - agent/cron/cron.go
  - agent/utils/clam/clam.go
  - agent/utils/clam/clam_security_test.go
  - agent/utils/xpack/helper/multi_node.go
  - frontend/src/views/toolbox/clam/index.vue
  - frontend/src/views/toolbox/clam/operate/index.vue
autonomous: true
requirements:
  - CLAM-01
user_setup:
  - A disposable Linux VPS with ClamAV and an isolated EICAR test directory is required for deferred acceptance.
must_haves:
  truths:
    - Persisted schedules survive agent restart and refresh their entry IDs.
    - Create/update/enable/disable/delete keep database and cron state consistent on success and failure.
    - Manual and scheduled scans of one rule cannot overlap.
    - Rule, scan, quarantine, and removal paths are normalized and confined.
    - Community users can view and edit durable schedules through the existing ClamAV UI.
    - Real ClamAV and EICAR behavior is manually accepted before CLAM-01 is closed.
  artifacts:
    - path: agent/app/service/clam.go
      provides: Durable lifecycle, restart restoration, validation, and execution claim.
    - path: agent/utils/clam/clam.go
      provides: Cron registration and secure quarantine operations.
    - path: agent/cron/cron.go
      provides: Startup schedule restoration.
    - path: frontend/src/views/toolbox/clam/operate/index.vue
      provides: Community schedule editing.
  key_links:
    - from: agent/cron/cron.go
      to: agent/app/service/clam.go
      via: RestoreClamSchedules at startup
    - from: agent/utils/clam/clam.go
      to: agent/app/service/clam.go
      via: registered HandleOnce callback
    - from: agent/app/service/clam.go
      to: database is_executing
      via: conditional atomic update
---

<objective>
Add durable, restart-safe, non-overlapping ClamAV schedules with confined quarantine paths.

Purpose: Implement the automatable portion of CLAM-01 without exposing unsafe deletion or concurrent scanning behavior.
Output: Scheduling lifecycle, restart restoration, atomic run claim, path protections, community UI access, tests, and a deferred VPS/EICAR checklist.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scheduled-clamav/03-CONTEXT.md
@agent/app/service/clam.go
@agent/utils/clam/clam.go
@agent/app/repo/clam.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make schedule lifecycle durable and restart-safe</name>
  <files>agent/app/service/clam.go, agent/cron/cron.go, agent/utils/clam/clam.go, agent/utils/xpack/helper/multi_node.go</files>
  <read_first>agent/app/service/clam.go, agent/app/repo/clam.go, agent/cron/cron.go, agent/utils/xpack/helper/multi_node.go</read_first>
  <action>Add standard cron validation and callback registration by persisted rule ID. Create the row before registering and roll it back on registration/update failure. For update and status changes, register the replacement first, persist its entry ID, then remove the old entry. Restore enabled schedules at startup, disable invalid rows, and save replacement entry IDs.</action>
  <verify>Run the focused schedule lifecycle/restart tests and full `go test ./...` from `agent` in Linux/WSL.</verify>
  <acceptance_criteria>
    - Create never registers an ID-zero rule.
    - Failed create/update leaves no active replacement entry.
    - Startup restore writes new entry IDs and disables invalid rules.
    - Agent tests exit 0.
  </acceptance_criteria>
  <done>Database and cron state remain consistent across lifecycle operations and restart.</done>
</task>

<task type="auto">
  <name>Task 2: Serialize scans and constrain paths</name>
  <files>agent/app/service/clam.go, agent/utils/clam/clam.go, agent/app/service/clam_schedule_test.go, agent/utils/clam/clam_security_test.go</files>
  <read_first>agent/app/service/clam.go, agent/utils/clam/clam.go, agent/app/repo/clam.go</read_first>
  <action>Use a conditional GORM update to claim `is_executing` and route both manual and scheduled starts through it. Reject update/status/delete during execution. Normalize names and absolute real directories, reject roots and overlap, confine quarantine paths below `1panel-infected`, reject symlinks/traversal, create directories with `0700`, and reconstruct safe removal paths.</action>
  <verify>Run schedule/concurrency/path safety tests and full `agent` tests.</verify>
  <acceptance_criteria>
    - Only one concurrent claim changes `is_executing` from false to true.
    - Root, traversal, symlink, and overlapping directories are rejected.
    - Quarantine directories are mode `0700`.
    - Removal cannot escape the validated rule directory.
  </acceptance_criteria>
  <done>Concurrent starts and unsafe filesystem targets are rejected before scan/deletion work begins.</done>
</task>

<task type="auto">
  <name>Task 3: Open schedule controls and record deferred EICAR acceptance</name>
  <files>frontend/src/views/toolbox/clam/index.vue, frontend/src/views/toolbox/clam/operate/index.vue</files>
  <read_first>frontend/src/views/toolbox/clam/index.vue, frontend/src/views/toolbox/clam/operate/index.vue, .planning/REQUIREMENTS.md</read_first>
  <action>Remove Pro-only visibility from schedule status/spec columns and schedule input. Preserve alert-specific license messaging. Add safe formatting for stored specs. Record restart, high-frequency overlap, and isolated EICAR procedures as human acceptance rather than claiming them from unit tests.</action>
  <verify>Run targeted ESLint for the two Vue files and `npm run build:pro`.</verify>
  <acceptance_criteria>
    - Community users can view and edit schedule specs.
    - Alert-only licensing conditions remain separate.
    - Targeted ESLint and production build exit 0.
    - EICAR/VPS checks remain explicitly deferred.
  </acceptance_criteria>
  <done>Community scheduling UI compiles and the remaining host-level acceptance scope is explicit.</done>
</task>

</tasks>

<verification>
- [x] Linux/WSL `agent`: `go test ./...`
- [x] Targeted ESLint for Phase 3 frontend files
- [x] `frontend`: `npm run build:pro`
- [ ] VPS restart and real cron timing (deferred)
- [ ] Isolated EICAR scan/quarantine test (deferred)
</verification>

<success_criteria>

- Automated lifecycle, concurrency, path, lint, and build gates pass.
- No test uses a production path or destructive EICAR setup.
- CLAM-01 remains pending until VPS restart and isolated EICAR acceptance pass.

</success_criteria>

<output>
Execution was committed in `fd6a1244476dbd25823653317e16058e708c4717`; the retrospective result is recorded in `03-01-SUMMARY.md`.
</output>
