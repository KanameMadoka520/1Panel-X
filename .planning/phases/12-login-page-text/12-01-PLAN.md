# Phase 12 Plan 01: Open Login-Page Text (+ login-hero fix)

**Requirement:** LOGIN-TEXT-01

## Steps

1. **`core/app/dto/enhancement.go`** — add `LoginWelcome/LoginSubtitle/Copyright` to `EnhancementSettingInfo` + `PublicEnhancementSettingInfo`; extend `EnhancementSettingUpdate.oneof`.
2. **`core/app/service/enhancement.go`** — populate the three in both getters; validator cases `LoginWelcome`/`LoginSubtitle` → `validateBrandingText(v,128)`, `Copyright` → `validateBrandingText(v,200)`.
3. **`core/app/service/enhancement_test.go`** — subset test stored map + `wantPublic` include the three; validator table adds accept (empty + valid, incl. CJK) and reject (markup, control char, over-length).
4. **Frontend store** — `store/interface/index.ts` type + `store/modules/global.ts` default add `loginWelcome/loginSubtitle/copyright`.
5. **Frontend populate** — `utils/xpack.ts` (`getXpackSettingForTheme`) + `global/use-logo.ts` set the three from the public response.
6. **`login/components/login-form.vue`** — welcome (`text-2xl`) + subtitle (`text-sm`) block at the top; copyright footer at the bottom; all `{{ }}` interpolation.
7. **`login/index.vue`** — LOGIN-HERO fix: reactive `watch(themeConfig fields, applyLoginBranding, { immediate: true })` replacing the one-shot onMounted preload.
8. **`setting/panel/index.vue`** — three `el-input` (maxlength 128/128/200) via `onSaveBranding`; form reactive + `search()` load.
9. **i18n** — `en.ts` + `zh.ts`: `loginWelcomeLabel/loginSubtitleLabel/copyrightLabel/copyrightHelper`.

## Verification
- gofmt/vet clean; focused `go test ./app/service -run 'Enhancement|Branding'`; full core `go test ./...` (revert x-log.json after).
- changed-file ESLint clean; `npm run build:pro`.
- Adversarial security review workflow over the diff.
- Live login-page render is human UAT (VPS/browser).

## Out of scope
- HTML/multi-line login text; multi-node.
