---
phase: 01-open-theme-and-watermark
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/app/api/v2/enhancement.go
  - core/app/api/v2/entry.go
  - core/app/dto/enhancement.go
  - core/app/service/enhancement.go
  - core/app/service/enhancement_test.go
  - core/app/service/setting.go
  - core/middleware/password_expired.go
  - core/middleware/password_expired_test.go
  - core/router/ro_setting.go
  - frontend/src/api/helper/check-status.ts
  - frontend/src/api/modules/enhancement.ts
  - frontend/src/extensions/theme.ts
  - frontend/src/extensions/xpack.ts
  - frontend/src/global/use-theme.ts
  - frontend/src/layout/components/Sidebar/components/Collapse.vue
  - frontend/src/layout/index.vue
  - frontend/src/routers/index.ts
  - frontend/src/store/modules/global.ts
  - frontend/src/utils/xpack.ts
  - frontend/src/views/setting/panel/index.vue
  - frontend/src/views/setting/panel/theme-color/index.vue
autonomous: true
requirements:
  - THEME-01
user_setup:
  - A browser-accessible 1Panel test instance is required for the deferred visual acceptance checks.
must_haves:
  truths:
    - Community administrators can save theme and watermark settings without a commercial license state.
    - The public settings response contains safe theme fields and excludes watermark content.
    - Invalid updates are rejected and invalid persisted values fall back to defaults.
    - Watermark toggling preserves one routed application tree.
    - Browser persistence and rendering are manually accepted before THEME-01 is closed.
  artifacts:
    - path: core/app/service/enhancement.go
      provides: Open validation, persistence, defaults, and public/full response separation.
    - path: core/router/ro_setting.go
      provides: Public and authenticated enhancement routes.
    - path: frontend/src/extensions/theme.ts
      provides: License-independent primary color application.
    - path: frontend/src/layout/index.vue
      provides: Single-tree watermark rendering.
  key_links:
    - from: frontend/src/api/modules/enhancement.ts
      to: core/router/ro_setting.go
      via: enhancement search and update HTTP endpoints
    - from: frontend/src/extensions/xpack.ts
      to: frontend/src/store/modules/global.ts
      via: validated open settings loaded into global theme state
    - from: frontend/src/store/modules/global.ts
      to: frontend/src/layout/index.vue
      via: watermark and theme configuration
---

<objective>
Implement a clean-room theme color and watermark path that works in the community build while preserving existing extension points.

Purpose: Deliver THEME-01 without changing product license state or exposing authenticated watermark data publicly.
Output: Open core endpoints and validation, frontend fallback integration, focused tests, and recorded automated verification with manual browser acceptance deferred.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-open-theme-and-watermark/01-CONTEXT.md
@core/app/service/setting.go
@frontend/src/extensions/xpack.ts
@frontend/src/global/use-theme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add validated open enhancement settings</name>
  <files>core/app/api/v2/enhancement.go, core/app/api/v2/entry.go, core/app/dto/enhancement.go, core/app/service/enhancement.go, core/app/service/setting.go, core/middleware/password_expired.go, core/router/ro_setting.go</files>
  <read_first>core/app/service/setting.go, core/router/ro_setting.go, core/middleware/password_expired.go</read_first>
  <action>Create public and authenticated enhancement DTOs and handlers. Persist Theme, ThemeColor, Watermark, and WatermarkShow through the settings repository. Validate theme modes, CSS colors, preset counts, watermark text and numeric bounds. Return only Theme and ThemeColor from `/api/v2/core/settings/enhancements/public`; protect full search and update endpoints with SessionAuth. Permit only the public endpoint through password-expired middleware.</action>
  <verify>Run `go test ./...` from `core` in Linux/WSL and inspect the public DTO serialization test.</verify>
  <acceptance_criteria>
    - `core/app/dto/enhancement.go` defines a public DTO without watermark fields.
    - `core/app/service/enhancement.go` rejects unsupported keys and unsafe color data.
    - `core/router/ro_setting.go` has one public GET plus authenticated search and update routes.
    - Core package tests exit 0.
  </acceptance_criteria>
  <done>Open settings are persisted and validated, public data is minimized, and core tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the existing settings UI to the open implementation</name>
  <files>frontend/src/api/helper/check-status.ts, frontend/src/api/modules/enhancement.ts, frontend/src/extensions/theme.ts, frontend/src/extensions/xpack.ts, frontend/src/global/use-theme.ts, frontend/src/layout/components/Sidebar/components/Collapse.vue, frontend/src/layout/index.vue, frontend/src/routers/index.ts, frontend/src/store/modules/global.ts, frontend/src/utils/xpack.ts, frontend/src/views/setting/panel/index.vue, frontend/src/views/setting/panel/theme-color/index.vue</files>
  <read_first>frontend/src/extensions/xpack.ts, frontend/src/global/use-theme.ts, frontend/src/layout/index.vue, frontend/src/views/setting/panel/index.vue</read_first>
  <action>Add the enhancement API client and use it as the fallback when private settings modules are absent. Remove the license gate from theme color and watermark controls. Apply primary CSS variables from open code, keep private theme hooks optional, and always render one `el-watermark` wrapper around the routed main container with empty content when disabled.</action>
  <verify>Run targeted ESLint on the changed frontend files and run `npm run build:pro` from `frontend`.</verify>
  <acceptance_criteria>
    - The panel settings page exposes theme color and watermark controls without checking `isProductPro`.
    - `frontend/src/extensions/theme.ts` sets Element Plus and panel primary color variables before optional private hooks.
    - `frontend/src/layout/index.vue` contains one routed `main-container` branch.
    - Targeted ESLint and the production build exit 0.
  </acceptance_criteria>
  <done>The community frontend loads, edits, and renders open theme/watermark settings and compiles successfully.</done>
</task>

<task type="auto">
  <name>Task 3: Record automated verification and deferred browser acceptance</name>
  <files>core/app/service/enhancement_test.go, core/middleware/password_expired_test.go</files>
  <read_first>core/app/service/enhancement.go, core/middleware/password_expired.go, .planning/REQUIREMENTS.md</read_first>
  <action>Add focused tests for valid and invalid settings, defaults, public response minimization, persisted-value fallback, legacy update validation, and password-expired route exposure. Record browser checks for refresh persistence, light/dark/system modes, watermark toggling, route-state preservation, and `color-mix()` rendering as deferred human acceptance rather than automated success.</action>
  <verify>Run `go test ./...` in `core`; confirm the phase verification status remains `human_needed` until browser checks are performed.</verify>
  <acceptance_criteria>
    - Focused tests cover public watermark exclusion and corrupt stored-value fallback.
    - Full core tests exit 0.
    - Browser checks are listed as deferred and are not marked passed.
  </acceptance_criteria>
  <done>Automated verification is complete and the remaining human acceptance scope is explicit.</done>
</task>

</tasks>

<verification>
Automated checks completed for the implementation revision:
- [x] Linux/WSL `core`: `go test ./...`
- [x] Targeted ESLint for Phase 1 frontend files
- [x] `frontend`: `npm run build:pro`
- [ ] Browser login, refresh, light/dark/system, watermark, route-state, and CSS color mixing checks (deferred)
</verification>

<success_criteria>

- The implementation tasks and automated checks are complete.
- No global Pro state or license response is forged.
- Public settings omit watermark data.
- THEME-01 remains pending until the documented browser acceptance checks pass.

</success_criteria>

<output>
Execution was committed in `bd2cb64d5dfaa31b05e2c1b3a2400376df84168d`; the retrospective result is recorded in `01-01-SUMMARY.md`.
</output>
