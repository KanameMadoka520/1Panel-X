---
phase: 04-ai-agent-soft-limit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/app/dto/setting.go
  - agent/app/service/agents.go
  - agent/app/service/agents_limit_test.go
  - agent/app/service/app.go
  - agent/app/service/setting.go
  - agent/i18n/lang/en.yaml
  - agent/i18n/lang/es-ES.yaml
  - agent/i18n/lang/fa.yaml
  - agent/i18n/lang/ja.yaml
  - agent/i18n/lang/ko.yaml
  - agent/i18n/lang/ms.yaml
  - agent/i18n/lang/pt-BR.yaml
  - agent/i18n/lang/ru.yaml
  - agent/i18n/lang/tr.yaml
  - agent/i18n/lang/zh-Hant.yaml
  - agent/i18n/lang/zh.yaml
autonomous: true
requirements:
  - AGENT-01
user_setup:
  - A VPS with sufficient Docker, CPU, memory, disk, and free ports is required for deferred multi-Agent acceptance.
must_haves:
  truths:
    - Missing or zero AIAgentLimit permits creation beyond the former count of five.
    - Values 1 through 1000 block creation at the configured count.
    - AI Agent installs bypass App Store metadata count limits without changing normal app behavior.
    - Existing creation validation and license state remain unchanged.
    - Live multi-Agent and concurrency behavior is manually assessed before AGENT-01 is closed.
  artifacts:
    - path: agent/app/service/agents.go
      provides: Optional soft limit and AI-only install hooks.
    - path: agent/app/service/app.go
      provides: Scoped App Store limit bypass.
    - path: agent/app/service/setting.go
      provides: AIAgentLimit validation.
    - path: agent/app/service/agents_limit_test.go
      provides: Limit and metadata-bypass unit tests.
  key_links:
    - from: agent/app/service/setting.go
      to: agent/app/service/agents.go
      via: AIAgentLimit settings repository value
    - from: agent/app/service/agents.go
      to: agent/app/service/app.go
      via: appInstallHooks SkipAppLimit
---

<objective>
Replace the fixed license-derived AI Agent count cap with an optional operator soft limit.

Purpose: Implement AGENT-01 without changing product license state and without a hidden App Store metadata cap.
Output: Settings contract, soft-limit enforcement, scoped metadata bypass, localized errors, automated tests, and explicit VPS/concurrency limitations.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ai-agent-soft-limit/04-CONTEXT.md
@agent/app/service/agents.go
@agent/app/service/app.go
@agent/app/service/setting.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add operator-configurable soft-limit semantics</name>
  <files>agent/app/dto/setting.go, agent/app/service/agents.go, agent/app/service/setting.go</files>
  <read_first>agent/app/service/agents.go, agent/app/service/setting.go, agent/app/dto/setting.go</read_first>
  <action>Add `AIAgentLimit` to settings DTO/update validation. Accept 0..1000 on update. Load the setting during Agent creation; treat missing or zero as unlimited and reject creation only when a positive limit is reached. Remove the fixed count of five and all license/xpack checks from this capacity decision while preserving the rest of Create validation.</action>
  <verify>Run focused capacity tests and full `go test ./...` from `agent` in Linux/WSL.</verify>
  <acceptance_criteria>
    - No `maxCommunityAIAgents` constant remains.
    - Zero limit returns no capacity error at counts above five.
    - Positive reached limit returns the localized error with the configured maximum.
    - Agent tests exit 0.
  </acceptance_criteria>
  <done>AI Agent count is controlled by the operator setting rather than product license state.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the secondary App Store metadata limit for AI installs</name>
  <files>agent/app/service/agents.go, agent/app/service/app.go, agent/app/service/agents_limit_test.go</files>
  <read_first>agent/app/service/agents.go, agent/app/service/app.go</read_first>
  <action>Add `SkipAppLimit` to private install hooks. Make ordinary installs enforce `checkRequiredAndLimit`; set `SkipAppLimit: true` for AI Agent creation only. Keep all other application install validation and lifecycle behavior unchanged. Test nil/default hooks and AI hooks separately.</action>
  <verify>Run `TestAgentInstallHooksSkipAppMetadataLimit` and full agent tests.</verify>
  <acceptance_criteria>
    - Nil and default hooks enforce App Store metadata limits.
    - AI Agent hooks bypass only the metadata limit check.
    - No public install API can set SkipAppLimit directly.
  </acceptance_criteria>
  <done>App metadata cannot silently restore the former AI count cap, and normal app limits remain active.</done>
</task>

<task type="auto">
  <name>Task 3: Generalize limit errors and document the soft-limit boundary</name>
  <files>agent/i18n/lang/en.yaml, agent/i18n/lang/es-ES.yaml, agent/i18n/lang/fa.yaml, agent/i18n/lang/ja.yaml, agent/i18n/lang/ko.yaml, agent/i18n/lang/ms.yaml, agent/i18n/lang/pt-BR.yaml, agent/i18n/lang/ru.yaml, agent/i18n/lang/tr.yaml, agent/i18n/lang/zh-Hant.yaml, agent/i18n/lang/zh.yaml</files>
  <read_first>agent/i18n/lang/en.yaml, agent/i18n/lang/zh.yaml, .planning/REQUIREMENTS.md</read_first>
  <action>Replace fixed community/five wording with a generic configured-limit message in all supported language files. Record that no dedicated UI exists, count-and-create is non-atomic, and unlimited count does not imply unlimited host resources. Leave live creation beyond five as deferred VPS acceptance.</action>
  <verify>Run full agent tests and verify all language files retain the `ErrAgentLimitReached` key; no frontend changes are required. Include the combined `npm run build:pro` gate in release verification.</verify>
  <acceptance_criteria>
    - Every modified language file has generic configured-limit wording.
    - Documentation names the concurrency and UI limitations.
    - Live multi-Agent tests are not marked passed.
  </acceptance_criteria>
  <done>Error output matches configurable limits and operational limitations are explicit.</done>
</task>

</tasks>

<verification>
- [x] Linux/WSL `agent`: `go test ./...`
- [x] Combined `frontend`: `npm run build:pro`
- [ ] Create Agents 6, 10, and 25 on a suitably sized VPS (deferred)
- [ ] Concurrent-create overshoot characterization (deferred; implementation is explicitly non-atomic)
- [ ] Dedicated settings UI (not implemented in this phase)
</verification>

<success_criteria>

- Unlimited and positive-limit unit behavior passes.
- Normal app limits remain active while AI metadata limits are bypassed.
- No license state changes.
- AGENT-01 remains pending until live VPS behavior is accepted.

</success_criteria>

<output>
Execution was committed in `c305d759133ae7e22f90f11e16921f0e722f9bed`; the retrospective result is recorded in `04-01-SUMMARY.md`.
</output>
